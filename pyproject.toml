[project]
name = "resume-generator"
version = "0.1.0"
description = "Generate resume HTML from YAML data"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "jinja2>=3.1.0",
]

[tool.uv]
dev-dependencies = []
